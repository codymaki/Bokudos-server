package com.bokudos.bokudosserver.enums;

/**
 * BASIC_WALKER: walks in one direction until it runs into a wall, then turns around and repeats. Falls off ledges.
 */
public enum EnemyType {
    BASIC_WALKER
}
